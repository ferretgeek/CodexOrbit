# 更新记录

[English](CHANGELOG.en.md)

本项目遵循语义化版本思路，但在 1.0 前后均可能因上游 Codex 协议变化调整兼容层。

## 3.2.1 - 2026-08-08

- `account/read` 反序列化后立即丢弃邮箱及无关账户字段，缓存只保留认证要求、
  账户类型和套餐类型。
- 本地 `error.log` 新增用户路径与常见密钥格式脱敏、单条长度上限和文件轮换，
  并在双语隐私与支持文档中公开其范围。
- 扩充当前 Schema 中 Business 与 Enterprise 套餐标识测试，并验证账户缓存
  最小化与诊断脱敏。
- Build 工作流只在 `main` 的 push/PR 运行，消除同一 PR 分支 push 与 PR 事件
  造成的重复构建和失败邮件。
- GitHub Actions 固定到完整提交 SHA，CI 固定为 Windows Server 2022 和
  Visual Studio 2022 工具链范围。
- 增加 C# CodeQL `security-extended` 扫描和 GitHub Release 构建来源证明。
- ZIP 使用稳定文件顺序和提交时间戳，确保相同源码、内容与工具链下打包结果稳定；
  不再宣称跨编译器版本二进制必然一致。
- Dependabot 的 GitHub Actions 更新按月合并为一个分组，并补齐全部中英双语
  文档与 v3.2.1 Release Notes。

## 3.2.0 - 2026-08-07

- 按当前 App Server Schema 合并稀疏的 `account/rateLimits/updated` 通知。
- 修复迷你窗内容宽度变化后越出屏幕，并保留最邻近的屏幕边缘锚点。
- 恢复停靠迷你窗时保留上次所在显示器。
- 实时服务失败后增加 15 秒退避，同时保留原有 30 秒最近快照缓冲。
- 固定 App Server 输入为无 BOM UTF-8，避免托管终端代码页破坏首条 JSONL 消息。
- 使用独立通知调度并直接发布实时快照，避免繁忙线程池延迟额度更新。
- 加载和保存前规范化设置，避免损坏或手改配置破坏界面。
- 修复退出与后台刷新之间的竞态。
- 确定性测试新增稀疏通知、本地日志、阈值、套餐、重置提醒和窗口定位覆盖。
- 补齐双语文档、支持与社区文件、Issue/PR 模板、Release Notes、校验值和标签发布。
- 为托管 CI 的冷启动提供更充足的假运行时初始化预算。
- 更新官方 GitHub Actions 到当前 v7 主版本。

## 3.1.2 - 2026-08-07

- 直接处理 `account/rateLimits/updated`，并保留 5 秒安全轮询。
- 支持 App、CLI、PATH、IDE 扩展、Windows 安装包缓存与 WSL。
- 主额度优先，模型专用 Spark 额度不再占用主界面。
- 主额度重置时间改为主要信息，显示剩余天数和小时。
- 详情改为右键触发，3 秒自动关闭，再次右键立即收起。
- 修复冷启动超时、账户重复读取、缓存完整性与异常处理。
- 设置保存不再生成 `.bak`。
- 增加服务端通知确定性测试。

## 未发布

未发布变更请记录在这里，再在 Release 时归档到具体版本。
